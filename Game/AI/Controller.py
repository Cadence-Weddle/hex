import random
import threading as threading
#from AI.main import Root, 

def MakeMove(computetime,board,humanplayer):
    return {'moveloc':random.choice([x for x in range(121)]),'gamestate':0}
'''
class AI(object):
	"""docstring for AI"""
	def __init__(self,):
		self.thread = threading.start_new_thread(main, )

'''