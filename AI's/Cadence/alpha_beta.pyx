from Tree_search cimport Node
from Heuristic import score

cdef class Node:
	def __init__(self, ):
		



cdef full_alpha_beta(node, unsigned long int alpha, unsigned long int beta, int curr_depth, int max_depth,
		maximising):
	if depth == max_depth:
		return score(Node)
	elif maxmising:
		next_layer = node.subnodes
		

cdef best_alpha_beta():


